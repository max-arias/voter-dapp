import { expect } from "chai";
import { ethers } from "hardhat";

describe("Voting", function () {
  it("Returns a list of active Elections", async function () {});
  it("Returns a list of past Elections", async function () {});
  it("Allows creating a new Election", async function () {});
});
